# NeedleProof contracts

`receipt.schema.json` is the portable receipt contract. Python models in `apps/api` are authoritative at runtime; `src/index.ts` supplies the matching browser-facing shapes used by the LiteShip interface.
